trigger:
- main

pool:
  name: 'reem'

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.10'

- script: make setup
  displayName: 'Create virtual environment'

- script: |
    source ~/.udacity-devops/bin/activate
    make all
  displayName: 'Activate Env and Run Makefile'